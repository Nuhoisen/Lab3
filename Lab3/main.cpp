#include <sdl.h>

#include <iostream>

#include "window.h"


int main(int argc, char *argv[])
{
	PVBC *mainHead[3];
	window * mainWindow = nullptr;
	surface * mainSurface=nullptr;


	for (int i = 0; i < 3; i++)
	{
		int number = (rand() % 2);

		switch (number)
		{
		case 0:			
		}
	}
}